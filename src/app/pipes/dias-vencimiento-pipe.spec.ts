import { DiasVencimientoPipe } from './dias-vencimiento-pipe';

describe('DiasVencimientoPipe', () => {
  it('create an instance', () => {
    const pipe = new DiasVencimientoPipe();
    expect(pipe).toBeTruthy();
  });
});
